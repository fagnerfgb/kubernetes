apiVersion: v1
kind: Pod
metadata:
  name: exec
  labels:
    app: exec
spec:
  containers:
  - name: exec
    image: busybox
    command:
      - /bin/sh
    args:
      - -c
      - touch /tmp/teste.txt; sleep 120;
  restartPolicy: Never